# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: url-shortener
'on':
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Install & run flake8
      run: 'pip install flake8

        flake8 app/ url_shortener/ --max-line-length=120 --exclude=migrations

        '
  test:
    runs-on: ubuntu-latest
    needs:
    - lint
    steps:
    - uses: actions/checkout@v4
    - name: Run Django tests
      run: 'pip install -r requirements.txt

        python manage.py test app --verbosity=2

        '
      env:
        DATABASE_URL: sqlite:///test.db
        SECRET_KEY: test-secret-key-not-real
        DEBUG: 'True'
        ALLOWED_HOSTS: '*'
  provision:
    runs-on: ubuntu-latest
    needs:
    - test
    steps:
    - uses: actions/checkout@v4
    - name: Terraform init
      run: "cd infra\nterraform init \\\n  -backend-config=\"bucket=${{ secrets.TF_STATE_BUCKET\
        \ }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME }}/terraform.tfstate\"\
        \ \\\n  -backend-config=\"region=us-east-1\"\n"
    - name: Terraform apply
      run: "cd infra\nterraform apply -auto-approve \\\n  -var=\"project_name=${{\
        \ secrets.PROJECT_NAME }}\" \\\n  -var=\"db_password=${{ secrets.DB_PASSWORD\
        \ }}\" \\\n  -var=\"secret_key=${{ secrets.DJANGO_SECRET_KEY }}\" \\\n  -var=\"\
        ssh_public_key=${{ secrets.SSH_PUBLIC_KEY }}\" \\\n  -var=\"tf_state_bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\"\n"
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    steps:
    - uses: actions/checkout@v4
    - name: Clone & start app
      run: "echo \"${{ secrets.SSH_PRIVATE_KEY }}\" > key.pem && chmod 600 key.pem\n\
        EC2_IP=$(cd infra && terraform init \\\n  -backend-config=\"bucket=${{ secrets.TF_STATE_BUCKET\
        \ }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME }}/terraform.tfstate\"\
        \ \\\n  -backend-config=\"region=us-east-1\" -reconfigure > /dev/null 2>&1\
        \ && \\\n  terraform output -raw ec2_public_ip)\nssh -o StrictHostKeyChecking=no\
        \ -i key.pem ${{ secrets.SSH_USER }}@$EC2_IP bash <<'ENDSSH'\n  set -euxo\
        \ pipefail\n  APP_DIR=/opt/url-shortener\n  REPO_URL=https://github.com/${{\
        \ secrets.GH_REPO }}.git\n\n  # Clone or pull\n  if [ -d \"$APP_DIR/.git\"\
        \ ]; then\n    cd \"$APP_DIR\" && git pull origin main\n  else\n    git clone\
        \ \"$REPO_URL\" \"$APP_DIR\"\n  fi\n\n  cd \"$APP_DIR\"\n\n  # Create/update\
        \ virtualenv\n  python3 -m venv venv\n  venv/bin/pip install --upgrade pip\n\
        \  venv/bin/pip install -r requirements.txt\n\n  # Migrate & collect static\n\
        \  venv/bin/python manage.py migrate --noinput\n  venv/bin/python manage.py\
        \ collectstatic --noinput\n\n  # (Re)start service\n  sudo systemctl restart\
        \ url-shortener\nENDSSH\n"
  verify:
    runs-on: ubuntu-latest
    needs:
    - configure
    steps:
    - uses: actions/checkout@v4
    - name: Health check
      run: "EC2_IP=$(cd infra && terraform init \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\" -reconfigure\
        \ > /dev/null 2>&1 && \\\n  terraform output -raw ec2_public_ip)\necho \"\
        Checking http://$EC2_IP/health/\"\nsleep 15\ncurl --fail --retry 8 --retry-delay\
        \ 10 --retry-all-errors \\\n  http://$EC2_IP/health/\n"
